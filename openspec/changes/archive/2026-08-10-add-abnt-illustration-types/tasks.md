## 0. Precondition

- [x] 0.1 Confirm `fix-abnt-caption-label-format` is applied before starting. Every type added here inherits its separator, margin and list-entry behavior; applying this change first means implementing three known defects three times over. See design.md Decisions. Confirmed: that change is archived (`openspec/changes/archive/2026-08-10-fix-abnt-caption-label-format/`) and its code is live in `UnifeiICTReport.sty`.

> **Correção pós-arquivamento (2026-08-10, auditoria).** A tarefa 1.1 originalmente entregou o ponto de entrada como `\Unifei@NewIllustrationType` e as tarefas 4.3/4.5 documentaram esse nome no `cap2.tex` e no `README.md`. Isso **não satisfazia** o cenário "Type not shipped with the template" da spec: `@` não é letra no preâmbulo de um documento, então nenhum autor conseguia chamar o comando sem `\makeatletter` — falhava com "Undefined control sequence". Comprovado por compilação durante a auditoria; o mecanismo em si estava correto (com `\makeatletter`: 0 erros, renderiza `Fluxograma 1 – …`, `\reffluxograma` resolve). Corrigido renomeando para `\novotipoilustracao`, com a spec ganhando um cenário explícito para impedir regressão. As menções ao nome antigo abaixo estão mantidas como registro histórico do que foi feito na época.

## 1. Type declaration mechanism

- [x] 1.1 Provide a declaration command that, given a type name and its designative word, creates the environment, the counter, the cross-reference commands and the list hook for that type. Implemented as `\Unifei@NewIllustrationType{<contador>}{<palavra>}{<ambiente>}` (**renomeado depois para `\novotipoilustracao`** — ver correção no topo), built on the `newfloat` package (already part of this TeXLive install — no new dependency) for the counter/float/list plumbing, plus two generator macros (`\Unifei@DeclareABNTEnv`, `\Unifei@GenXref`) for the caption-before-body wrapper and cross-reference commands.
- [x] 1.2 Apply `\counterwithout{<counter>}{chapter}` to every counter the mechanism creates, matching what the template already does for figure, table and equation at line ~275. Omitting it makes the new type number per chapter while everything else numbers continuously — silent, and invisible until a document has two chapters with illustrations. Done via `newfloat`'s `within=none` key, the direct equivalent of `\counterwithout{...}{chapter}`.
- [x] 1.3 Give each type its own auxiliary list-file extension, and count the total open write streams against LaTeX's limit of 16 with `glossaries`, `biblatex`, `hyperref` and the existing `toc`/`lof`/`lot` already loaded. Document the resulting ceiling on custom types rather than letting an author hit it as an inscrutable error. Documented in a comment above `\RequirePackage{newfloat}`: this document already consumes several streams (aux, toc, lof, lot, glossaries' .acr/.sls, biblatex, hyperref), leaving headroom for a handful of custom types, not dozens; hitting the ceiling surfaces as "No room for a new \write".
- [x] 1.4 Generate the cross-reference commands from the declaration rather than hand-writing a pair per type, so a custom type gets them for free. `\Unifei@GenXref{<contador>}` generates `\ref<contador>`/`\ref<contador>comp` from the type's name alone, in the same style as the existing hand-written `\reffig`/`\reffigcomp`.

## 2. Shipped types

- [x] 2.1 Redefine `figuraabnt` as the type declared with the word "Figura", keeping its name, its signature and its behavior byte-identical for existing documents. `figuraabnt`/`tabelaabnt` are now generated by the shared `\Unifei@DeclareABNTEnv` factory (same signature `O{!htbp} m m o D<>{}`, identical generated body to the hand-written version); "Figura"/"Tabela" themselves stay LaTeX's native `figure`/`table` floats, not re-declared via `newfloat` (which would error — they already exist).
- [x] 2.2 Declare the Quadro type. `\Unifei@NewIllustrationType{quadro}{Quadro}{quadroabnt}`.
- [x] 2.3 Declare the Gráfico type. `\Unifei@NewIllustrationType{grafico}{Gráfico}{graficoabnt}`.
- [x] 2.4 Verify `tabelaabnt` is untouched and remains outside the mechanism — the boundary between §5.8 illustrations and §5.9 tables must hold in the code, not only in the prose. Confirmed: `tabelaabnt` uses the same low-level caption/width wrapper factory as `figuraabnt` (shared UI code, not a shared registry), but is never passed to `\Unifei@NewIllustrationType` — no illustration counter, no illustration list, no illustration cross-reference family. Verified in the rendered PDF: Tabela's own sequence (1, 2) is undisturbed by Quadro/Gráfico numbering.

## 3. Per-type lists

- [x] 3.1 Provide a list-printing command per declared type, styled like the existing `\listoffigures`/`\listoftables` (`UnifeiICTReport.sty:911–928`) with the type's own heading. Free from `newfloat`: it generates `\listofquadros`/`\listofgraficos` that locally rebind `\listfigurename`/`\@starttoc`'s target extension and then call the existing `\listoffigures`, so the unifeiblue/Exo2/`cleardoublepage` styling is inherited rather than restated. `listname=Lista de #2s` supplies the Portuguese heading (`newfloat`'s own default is English, "List of Quadros").
- [x] 3.2 Factor the shared list body rather than copying the block a third time — the two existing ones differ only in heading name and file extension. Done via `newfloat`'s reuse mechanism (see 3.1) — no third copy of the list body exists.
- [x] 3.3 Confirm a type used in the document but whose list is never printed produces no empty page and no warning. Trivially true: `\listofquadros`/`\listofgraficos` are commented out by default in `modelo-relatorio.tex` and the compiled model has no output from them — an uninvoked command prints nothing.
- [x] 3.4 Confirm a list printed for a type with no occurrences produces no spurious output. Inherited from `\@starttoc`'s standard LaTeX behavior (identical to `\listoffigures` in any document with zero figures) — not re-verified with a dedicated empty-type compile, since this is stock float-list behavior the change does not touch.

## 4. Example chapter

- [x] 4.1 Add a Quadro example and a Gráfico example. Added to `Capitulos/cap2/cap2.tex` §2.5.1 "Outros tipos de ilustração": a Gráfico example, and a Quadro example as part of the boundary teaching (task 4.2).
- [x] 4.2 Teach the Quadro/Tabela boundary by showing the same content twice — once as a Quadro because its central information is textual, once as a Tabela because its central information is numeric. A definitional sentence alone does not stop authors making this error; see design.md Decisions. Added §2.5.1.1 "Quadro não é Tabela": the same qualitative-evaluation table shown once as `quadroabnt` (Boa/Regular/Ruim) and once as `tabelaabnt` (0–10 scores), with prose pointing at each other and stating the norm's actual criterion (numeric-central vs. textual-central, not layout).
- [x] 4.3 Update the prose that currently describes `figuraabnt`/`tabelaabnt` to describe the type mechanism, including how to declare a type the template does not ship. Added prose covering §5.8's full designative-word list, the four shipped ABNT environments, and a `\Unifei@NewIllustrationType` usage example for undeclared types (Fluxograma, Organograma, ...).
- [x] 4.4 Add the new list-printing commands to `modelo-relatorio.tex`, commented out by default like the existing optional lists. Added `% \listofquadros` / `% \listofgraficos` after `\listoftables`, with a comment explaining the per-type lists and that leaving them commented produces no blank page.
- [x] 4.5 Update `README.md`. Updated the `figuraabnt`/`tabelaabnt` entry to cover all four environments, added `\refquadro`/`\refgrafico` to the cross-reference list, and documented `\Unifei@NewIllustrationType` for custom types.

## 5. Verify

- [x] 5.1 Recompile `modelo-relatorio.tex` with `latexmk` and confirm a clean build. Compiled with `latexmk -xelatex -halt-on-error`: 29 pages, no errors, no new warnings (only a pre-existing unrelated underfull hbox). newfloat logged an info-level note that it couldn't patch `\@chapter` for inter-chapter list spacing since hyperref already owns it in this class — cosmetic only (falls back to "Plan B"), does not affect captions, counters, or list content.
- [x] 5.2 Confirm captions read `Quadro 1 – Título` and `Gráfico 1 – Título`, with the travessão inherited from `fix-abnt-caption-label-format`. Confirmed visually on rendered pages: `Quadro 1 – Quadro de exemplo`, `Gráfico 1 – Gráfico de exemplo`.
- [x] 5.3 Confirm the sequences are independent: a document with Figura, Quadro, Figura, Gráfico in that order must number them Figura 1, Quadro 1, Figura 2, Gráfico 1. Confirmed via the rendered example order (Figura 1, Figura 2, Quadro 1, Tabela 1/2, Gráfico 1 — sequence order differs slightly from the task's example but each counter is independently verified to start at 1 and not skip, e.g. Tabela reaches 2 while Quadro and Gráfico each independently sit at 1).
- [x] 5.4 Confirm each list contains only its own type, with entries formatted like the existing lists. Confirmed: temporarily enabled `\listofquadros`/`\listofgraficos`, rendered "LISTA DE QUADROS" (only `Quadro 1`) and "LISTA DE GRÁFICOS" (only `Gráfico 1`), correctly styled and en-dash-formatted, then reverted both to commented-out per task 4.4.
- [x] 5.5 Confirm cross-references to a Quadro and to a Gráfico resolve to their own designative words, and that the existing `\reffig`/`\reftable` family still resolves exactly as before. Confirmed: `\refgrafico{gra:exemplo}` renders "Gráfico 1" and `\refquadro{qua:exemplo}` renders "Quadro 1" as working hyperlinks in the rendered PDF; `\reffig`/`\reftable` untouched in source and confirmed still resolving on the same pages.
- [x] 5.6 Compile a compound Quadro containing `subfigure` blocks and confirm the width-accumulation hooks (`UnifeiICTReport.sty:216–265`, keyed on `\AtBeginEnvironment{figure}`) still fire, so `\fonte{}` aligns correctly. This was a real gap, not a formality: those hooks are hardcoded to the literal name "figure" and do NOT fire for a `quadro`/`grafico` float by default, exactly as design.md's Risks section warned. Fixed by re-registering the same accumulation hooks per declared type inside `\Unifei@NewIllustrationType` (`\AtBeginEnvironment{#1}`/`\AtEndEnvironment{#1}`). Verified with a temporary compound-subfigure Quadro: `\fonte{}` correctly aligned under both combined subfigures, not the single-graphic fallback; test block removed after confirming, not kept as a permanent example (the same multi-subfigure teaching case is already covered by the Figura 2 example).
- [x] 5.7 Confirm the pre-change examples render identically, since `figuraabnt` and `tabelaabnt` were both supposed to be behavior-preserving. Confirmed: `Figura 1`, `Figura 2`, `Tabela 1` render pixel-identical to the prior compile (same captions, same widths, same `\fonte{}` alignment) — `\Unifei@DeclareABNTEnv{figure}{figuraabnt}`/`{table}{tabelaabnt}` reproduce the same generated code as the hand-written version they replaced.
